services:
  mongodb:
    image: mongo:7-jammy
    container_name: ragforge-mongodb
    restart: always

    ports:
      - '27007:27017'

    volumes:
      - ./ragforge_mongodb_data:/data/db

    networks:
      - backend

networks:
  backend: